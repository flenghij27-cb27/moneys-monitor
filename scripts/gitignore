__pycache__/
*.py[cod]
.venv/
venv/
.env
*.tmp
.DS_Store
.idea/
.vscode/

# ---------------------------------------------------------------------
# DATI PERSONALI — la repo e' PUBBLICA.
# transactions.csv contiene i tuoi importi e le tue date: non deve uscire.
# config.json invece contiene solo ticker e pesi obiettivo: puoi tenerlo
# versionato senza rivelare nulla di personale.
#
# ATTENZIONE: .gitignore vale per git da riga di comando. Se crei un file
# dall'interfaccia web di GitHub, .gitignore NON ti ferma. Non creare mai
# pac/transactions.csv dal browser.
# ---------------------------------------------------------------------
pac/transactions.csv
